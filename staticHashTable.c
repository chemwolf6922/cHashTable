#include "staticHashTable.h"
#include <malloc.h>

typedef struct{
    
}static_hash_table_t;

int static_hash_table_create(static_hash_table_handle_t* handle,hash_table_item* items,size_t len)
{
    *handle = NULL;
    return -1;
}

void* static_hash_table_get(static_hash_table_handle_t* handle,uint8_t* key,size_t key_len)
{
    return NULL;
}